# projectwin-site

The public site for Project Win LLC at [projectwin.cloud](https://projectwin.cloud). Astro, Three.js, GSAP.

```bash
npm ci
npm run dev          # local dev server
npm run check && npm test && npm run build && npm run budget && npm run e2e
```

See `AGENTS.md` for conventions and deploy notes.
